{% extends 'base.html.twig' %}

{% block title %}Edit Checkpoint{% endblock %}

{% block body %}
    <h1>Edit Checkpoint</h1>

    {{ include('back/checkpoint/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('back_checkpoint_index') }}">back to list</a>

    {{ include('back/checkpoint/_delete_form.html.twig') }}
{% endblock %}
