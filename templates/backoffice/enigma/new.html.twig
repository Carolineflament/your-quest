{% extends 'base.html.twig' %}

{% block title %}Nouvelle énigme pour le checkpoint {{ checkpoint.title }}{% endblock %}

{% block body %}
    {{ include('_header_start.html.twig', {'title': 'Nouvelle énigme' }) }}

    <div class="container-xxl pb-5">
        <div class="container">
            <div class="d-flex flex-row-reverse bd-highlight align-items-start">
                <a href="{{ path('app_backoffice_checkpoint_show', {'id': checkpoint.id}) }}" class="btn btn-outline-secondary btn-sm">Retourner au checkpoint</a>
            </div>

            {{ include('backoffice/enigma/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
