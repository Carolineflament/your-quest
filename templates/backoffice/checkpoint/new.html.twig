{% extends 'backoffice/back_base.html.twig' %}

{% block title %}Nouveau checkpoint pour le jeu {{ game.title }}{% endblock %}

{% block back_body %}
    <div class="d-flex flex-row-reverse bd-highlight align-items-start">
        <a href="{{ path('app_backoffice_checkpoint_index', {'gameSlug': game.slug }) }}" class="btn btn-outline-secondary btn-sm">Retourner à liste</a>
    </div>

    {{ include('backoffice/checkpoint/_form.html.twig') }}
{% endblock %}
