{% extends 'base.html.twig' %}

{% block title %}Checkpoint{% endblock %}

{% block body %}
    <h1>Checkpoint</h1>

    <div class="d-grid gap-2 d-md-flex justify-content-md-start">

     <a href="{{ path('app_backoffice_checkpoint_index', {'gameSlug': game.slug }) }}" class="btn btn-outline-secondary btn-sm">back to list</a>
    </div> 

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ checkpoint.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ checkpoint.title }}</td>
            </tr>
            <tr>
                <th>SuccessMessage</th>
                <td>{{ checkpoint.successMessage }}</td>
            </tr>
            <tr>
                <th>OrderCheckpoint</th>
                <td>{{ checkpoint.orderCheckpoint }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ checkpoint.createdAt ? checkpoint.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ checkpoint.updatedAt ? checkpoint.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

        <div class="d-grid gap-2 d-md-flex justify-content-md-center">
            <a href="{{ path('app_backoffice_checkpoint_edit', {'id': checkpoint.id}) }}" class="btn btn-success">Modifier</a>
        </div>    

    {{ include('backoffice/checkpoint/_delete_form.html.twig') }}
{% endblock %}
