{% extends 'base.html.twig' %}

{% block title %}Checkpoint{% endblock %}

{% block body %}
    <h1>Checkpoint</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ checkpoint.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ checkpoint.title }}</td>
            </tr>
            <tr>
                <th>SuccessMessage</th>
                <td>{{ checkpoint.successMessage }}</td>
            </tr>
            <tr>
                <th>OrderCheckpoint</th>
                <td>{{ checkpoint.orderCheckpoint }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ checkpoint.createdAt ? checkpoint.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ checkpoint.updatedAt ? checkpoint.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

     <a href="{{ path('backoffice_checkpoint_index') }}" class="btn btn-outline-secondary btn-sm">back to list</a>

    <a href="{{ path('backoffice_checkpoint_edit', {'id': checkpoint.id}) }}" class="btn btn-primary">Modifier</a>

    {{ include('backoffice/checkpoint/_delete_form.html.twig') }}
{% endblock %}
