<form method="post" action="{{ path('backoffice_checkpoint_delete', {'id': checkpoint.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ checkpoint.id) }}">
    <div class="d-grid gap-2 d-md-flex justify-content-md-center">
    <button class="btn btn-danger">{{ button_label|default('Supprimer') }}</button>
    </div>
</form>
