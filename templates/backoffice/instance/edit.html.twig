{% extends 'base.html.twig' %}

{% block title %}Edition de la session {{ instance.title }} du jeu {{ game.title }}{% endblock %}

{% block body %}
    {{ include('_header_start.html.twig', {'title': 'Edition de la session '~instance.title}) }}
    <div class="container-xxl pb-5">
        <div class="container">
            <div class="d-flex flex-row-reverse bd-highlight align-items-start">
                <a href="{{ path('app_backoffice_game_show', {'slug': game.slug}) }}"  class="btn btn-outline-secondary btn-sm">Retourner au jeu</a>
            </div>

            {{ include('backoffice/instance/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>
{% endblock %}
