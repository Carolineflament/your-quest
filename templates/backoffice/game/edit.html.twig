{% extends 'backoffice/back_base.html.twig' %}

{% block title %}Modifier le jeu : {{game.title}}{% endblock %}

{% block back_body %}
    <div class="d-flex flex-column align-items-end">
        <a href="{{ path('app_backoffice_game_index') }}" class="btn btn-primary">Mes jeux</a>
    </div>
    <div class="clearfix"></div>
    {{ include('backoffice/game/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

{% endblock %}
