{% extends 'base.html.twig' %}

{% block title %}Mon jeu{% endblock %}

{% block body %}
    <div class="container-fluid mb-3">
        <h1>Mon jeu : {{ game.title }}</h1>

    
        <div class="card mb-3">
            <img src="{{ game.image }}" class="card-img-top" alt="...">
            <div class="card-body">
                <h5 class="card-title">{{ game.title }}</h5>
                <p class="card-text">{{ game.summary }}</p>
                <p class="card-text"><small class="text-muted">Lieu: {{ game.address }} - {{ game.postalCode }} {{ game.city }}</small></p>
                <div class="d-grid gap-2 justify-content-md-center">
                    {{ include('backoffice/game/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
   
        <a href="{{ path('app_backoffice_game_index') }}">Voir tous mes jeux</a>

        <a href="{{ path('app_backoffice_game_edit', {'id': game.id}) }}">modifier</a>
    <div>
{% endblock %}
