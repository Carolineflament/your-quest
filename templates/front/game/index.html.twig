{% extends 'base.html.twig' %}

{% block body %}

    <!-- Page Header Start -->
    <div class="container-fluid page-header py-5 mb-5">
        <div class="container py-5">
            <h1 class="display-3 text-white mb-3 animated slideInDown">Les jeux</h1>
            {{ include('_breadcrumb.html.twig') }}
        </div>
    </div>
    <!-- Page Header End -->

    <!-- Service Start -->
    {% set delay = [0.1, 0.3, 0.5] %}
    <div class="container-xxl py-5">
        <div class="container">
            <div class="row g-4">
                {% for game in games %}
                    {% set counter = ( counter | default(-1) ) + 1 %}
                    {% if counter == 3 %}
                        {% set counter = 0 %}
                    {% endif %}
                    <div class="col-md-6 col-lg-4 wow fadeInUp" data-wow-delay="{{delay[counter]~'s'}}">
                        <div class="service-item">
                            <div class="overflow-hidden">
                                <img class="img-fluid" src="{{ asset(app_games_images_directory~game.image) }}" alt="{{game.slug}}">
                            </div>
                            <div class="p-4 text-center border border-5 border-light border-top-0">
                                <h4>{{ game.title }}</h4>
                                <div class="small mb-3">
                                    <address class="mb-0">
                                        <i class="bi bi-house-fill me-2"></i><a href="http://maps.google.com/?q={{ game.address }} {{ game.postalCode }} {{ game.city }}" target="_blank">{{ game.address }} {{ game.postalCode }} {{ game.city }}</a>
                                    </address>
                                </div>
                                <p>{{ game.summary}}</p>
                                <a class="fw-medium" href="{{ path('front_games_show', {'slug' : game.slug}) }}">Accéder au jeu<i class="fa fa-arrow-right ms-2"></i></a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
            {{ include('_paginate.html.twig') }}
        </div>
    </div>
    <!-- Service End -->
{% endblock %}
